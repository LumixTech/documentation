# CI / integration test profili. Bağlantılar Testcontainers tarafından runtime'da enjekte edilir.
spring:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
  kafka:
    consumer:
      auto-offset-reset: earliest

logging:
  level:
    com.lumix: DEBUG
