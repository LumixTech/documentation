# Local development. `docker compose` ile Postgres/Kafka localhost'ta beklenir.
spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.lumix: DEBUG
    org.springframework.web: DEBUG

management:
  endpoint:
    health:
      show-details: always
